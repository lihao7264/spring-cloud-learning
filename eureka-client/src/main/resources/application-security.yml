server:
  port: 8103
spring:
  application:
    name: eureka-client
eureka:
  client:
    register-with-eureka: true  #注册到Eureka注册中心
    fetch-registry: true   #获取注册服务列表
    service-url:  #服务地址
      defaultZone: http://lihao:123456@localhost:8004/eureka/